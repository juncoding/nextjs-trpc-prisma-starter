---
description: "Scaffold a new lightweight internal management system on Next.js + tRPC + Prisma in SPA mode. Interactive Q&A flow that generates the canonical folder structure, sample tRPC router, sample service, Prisma schema starter, .env.example, and CLAUDE.md."
---

Invoke the `nextjs-trpc-prisma-starter:scaffold-internal-tool` skill and follow it exactly as presented to you. The user wants to start a brand-new project on this stack — walk them through the interactive Q&A (project name, location, database, auth, cache, MCP, deployment target) and then generate the project.
